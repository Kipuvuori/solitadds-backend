machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get dist-upgrade
    - sudo apt-get install python-tk tk python-tk-dbg tk-dev python-pandas
    - pip install -r requirements.txt
