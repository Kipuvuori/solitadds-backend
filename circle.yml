machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install python-tk tk python-tk-dbg tk-dev
    - python setup.py build_ext --inplace
    - pip install -r requirements.txt
